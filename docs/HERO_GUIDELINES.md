# Hero guidelines

The README hero should stay readable at GitHub README width.

Rules:

- Use SVG with real text.
- Avoid blur filters and tiny labels.
- Keep the pipeline metaphor simple: raw → records → bundles → agent.
- Do not include screenshots or real vault content.
